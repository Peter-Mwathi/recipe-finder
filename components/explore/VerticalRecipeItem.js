import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VerticalRecipeItem = ({item}) => {
  return (
    <View>
      <Text>name</Text>
    </View>
  )
}

export default VerticalRecipeItem

const styles = StyleSheet.create({})